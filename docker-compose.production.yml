version: '3.8'

services:
  family-task-tracker:
    image: ghcr.io/soopahfly/family-task-tracker:latest
    container_name: family-task-tracker
    ports:
      - "3000:80"
    restart: unless-stopped
    volumes:
      - family-task-data:/app/server

volumes:
  family-task-data:
